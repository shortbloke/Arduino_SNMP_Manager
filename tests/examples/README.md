# Original example compilation

Run `pio run -d tests/examples`. Wrappers include the unchanged 1.1.13 sketches:
the Wi-Fi examples on ESP8266/ESP32 and the Ethernet example on ESP8266 with the
Ethernet library. Compile success does not certify network or application behavior.
The original examples retain their existing calculation and polling conventions.
