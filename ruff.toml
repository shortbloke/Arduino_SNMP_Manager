# Keep lint policy local to the repository, independent of contributor settings.
target-version = "py312"
line-length = 100
extend-exclude = [".pio", "build", "build-*"]

[lint]
select = ["E4", "E7", "E9", "F", "I", "B"]
